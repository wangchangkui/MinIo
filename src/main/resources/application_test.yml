server:
  port: 9000
spring:
  application:
    name: minioUtil

minio:
  endpoint: your-endpoint
  accessKey: your-access-key
  secretKey: your-secret-key
  nginxHost: your-nginx-host
  maxSize: 10240
